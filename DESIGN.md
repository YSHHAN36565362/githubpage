# DESIGN.md — 한윤수 포트폴리오 사이트

> 이 저장소(`index.html` / `style.css` / `script.js` / `anime.min.js`)의 **설계 기준 문서**입니다.
> 디자인이나 콘텐츠를 바꿀 때는 **이 문서를 먼저 고치고, 그다음 코드를 여기에 맞추세요.**
> 코드를 먼저 고치면 다음 사람이(=미래의 나) 왜 그렇게 되어 있는지 알 수 없게 됩니다.

최종 갱신: 2026-08-13

---

## 1. 목적과 대상

| 항목 | 내용 |
|---|---|
| 목적 | 테크노프로(株式会社テクノプロ コンサルティング・パートナーズ) システムエンジニア 지원 시 제출하는 자기소개 페이지 |
| 읽는 사람 | 일본 기업 채용 담당자 (일본어 우선), 한국 기업/지인 (한국어), 그 외 (영어) |
| 성격 | 이력서가 아니라 **"어떤 사람이고 무엇을 만들었는가"를 2분 안에 보여주는 페이지** |
| 배포 | GitHub Pages (`https://yshhan36565362.github.io/githubpage/`) |

**포트폴리오는 2건으로 고정합니다.**
1. 학술논문 연구분야 자동분류 모델 (KoBERT)
2. KMOVE 단어장 앱 (망각곡선 기반)

> SafeCity, 구인구직 대시보드 등은 의도적으로 **제외**했습니다.
> 자기소개서(ver.7)에서도 포트폴리오를 이 2건으로 좁혔기 때문에, 서류와 사이트의 메시지를 일치시킵니다.

---

## 2. 콘텐츠의 출처 (진실의 원천)

새 문구를 쓸 때는 반드시 아래 원본을 먼저 확인하세요. 사이트에서 지어내지 않습니다.

| 섹션 | 근거 파일 |
|---|---|
| 모토, 자기소개 서사, 장·단점 | `~/Downloads/Claude/Technopro_design_ES_ver.7.txt` (2026-08-13) |
| 학력 / 기술스택 / 수상 | `GitHub/Introduce/introduce.md` |
| 프로젝트 01 (논문 분류) | `univ-data-science-portfolio/2025-2/kobert-paper-classifier/README.md` |
| 프로젝트 02 (단어장 앱) | `GitHub/word_test/README.md` |

### 톤 규칙
- 자기소개서 문장을 **그대로 붙여넣지 않습니다.** 경어체 이력서 문장은 웹에서 무겁게 읽힙니다.
  원본의 사실과 뉘앙스만 가져와 포트폴리오 톤으로 다시 씁니다.
- 3개 언어 모두 **각각 따로 작성**합니다. 기계번역식 직역 금지.
- 병역 에피소드, 아르바이트 경력(교보문고·서울아산병원)은 **사이트에 넣지 않습니다.**
  분량이 늘고 이력서처럼 보이며, 두 프로젝트에 집중하는 목적이 흐려집니다. (면접·서류에서 다룰 내용)
- 논문 분류 프로젝트는 자기소개서에서는 "리더로서의 진행 방식" 중심으로 썼지만,
  **이 사이트는 포트폴리오이므로 수치(Acc 0.9691 / Macro-F1 0.956)를 유지**하고 리더십 서술을 한 줄 덧붙입니다.

---

## 3. 참고한 레퍼런스

| 레퍼런스 | 가져온 것 |
|---|---|
| Colorlib — *21 Best Developer Portfolio Websites (2026)* | 1페이지 구성, 프로젝트를 카드가 아닌 **번호 매긴 세로 목록**으로 나열하는 방식 |
| wallofportfolios.in (dark-theme / minimal 컬렉션) | 여백과 타이포 위주의 절제된 다크 톤, 강조색은 1~2개로 제한 |
| SimpleLocalize / Linguise — *language switcher UX* | 언어 전환은 **국기 아이콘이 아니라 각 언어의 자체 표기**(日本語 / 한국어 / EN)로, 상단 고정 |
| animejs.com — *Animation* 문서의 `splitText` + `stagger` 예제 | 히어로 이름의 글자 단위 등장 연출 (원본 예제의 무한 loop·bounce는 제거하고 1회·짧게 조정) |

---

## 4. 디자인 토큰

바꿀 일이 있으면 **`style.css` 상단 `:root` 한 곳만** 고치면 전체에 반영됩니다.

```
배경   --bg #080b14   --bg-elev #0e1322   --card #111828
경계   --border #1e2740   --border-soft #172033
텍스트 --text #eef1f8   --text-dim #b3bbd0   --muted #7c869f
강조   --accent #22d3ee (시안)   --accent-2 #818cf8 (인디고)
       --grad linear-gradient(120deg, #818cf8, #22d3ee)
```

### 강조색 사용 규칙
- 그라데이션(`--grad`)은 **의미가 있는 곳에만**: 섹션 번호, 프로젝트 번호, 지표 수치,
  기본 버튼, 선택된 언어 버튼, 스크롤 진행 바. 남발하면 싸구려로 보입니다.
- 본문 텍스트에는 그라데이션을 쓰지 않습니다.
- 링크·강조 아이콘은 단색 `--accent`, 태그·보조 강조는 `--accent-2`.

### 레이아웃
- 콘텐츠 최대 너비 `--maxw: 1060px`, 섹션 상하 패딩 76px, 섹션 사이는 1px 실선으로 구분.
- 본문 문단은 `max-width: 64~66ch` — 한 줄이 너무 길면 읽기 어렵습니다.

---

## 5. 정보 구조

```
상단 고정 바   브랜드(HY.) / 섹션 메뉴 / 언어 전환 [日本語][한국어][EN]
              + 최상단 스크롤 진행 바
─────────────────────────────────────────
Hero          eyebrow · 이름 · 한 줄 소개 · 모토 인용 · CTA 2개 + 궤도 그래픽
01 About      프로필 카드(sticky) + 소개 문단 + 학력/스킬/수상 3카드
02 Projects   번호 + 제목 + 기간 + 설명 + 스택 + 지표 3개 + 성과 + 링크
03 Tech Stack 태그 나열
04 Contact    Email / GitHub 카드
Footer
```

프로젝트를 카드 그리드가 아니라 **번호 붙은 세로 목록**으로 둔 이유:
2건뿐이라 그리드로 만들면 화면이 휑하고, 세로 목록이 각 프로젝트의 설명을 충분히 넣을 수 있습니다.

---

## 6. i18n (3개 국어)

- 기본 언어 **일본어**. `한국어`, `EN` 버튼으로 전환. 마지막 선택은 `localStorage`에 저장.
- 번역 텍스트는 외부 JSON이 아니라 **`script.js` 안의 `translations` 객체**에 둡니다.
  → `fetch`가 필요 없어 CORS·네트워크 실패에 영향받지 않고, 로컬에서 파일을 더블클릭해도 동작합니다.
- 전환 시 `<html lang>` 과 `document.title` 도 함께 바뀝니다 (SEO·스크린리더).
- 고유명사(Python, PyTorch…)와 숫자·날짜는 언어와 무관하게 고정합니다.

### 문구를 고칠 때
`script.js` 의 `translations.ja / .ko / .en` **세 곳을 모두** 고쳐야 합니다.
한 곳만 고치면 다른 언어에서 옛 문구가 남습니다.

---

## 7. 애니메이션

| 대상 | 방식 |
|---|---|
| 히어로 이름 | anime.js `splitText` + `stagger` — 글자가 순서대로 올라오며 등장 (언어 전환 시 재생) |
| 히어로 나머지 | anime.js `stagger` 페이드 업 |
| 궤도 노드 | 등장은 anime.js `opacity`, 떠다니는 움직임은 **CSS `bob` 키프레임** |
| 궤도 링 | anime.js `scale` + `opacity` |
| 섹션 등장 | IntersectionObserver + CSS transition |
| 프로젝트 지표 | 행이 화면에 들어오면 anime.js `stagger` 로 순차 강조 |
| 스크롤 진행 바 | 순수 JS (`scaleX`) |

### ⚠️ 애니메이션 3대 주의사항 (실제로 겪은 버그)

1. **`transform` 을 중앙정렬에 쓰지 말 것.**
   `.ring` 을 `top:50%; left:50%; transform:translate(-50%,-50%)` 로 두면,
   anime.js가 `scale` 을 애니메이션할 때 `transform` 을 통째로 덮어써서 링이 중앙에서 튕겨나갑니다.
   → `inset: 0; margin: auto;` 로 중앙정렬합니다.

2. **같은 요소의 `transform` 을 CSS 애니메이션과 anime.js 가 동시에 건드리지 말 것.**
   `.node` 는 CSS `bob` 키프레임이 `transform` 을 쓰므로, anime.js 는 `opacity` 만 다룹니다.

3. **`prefers-reduced-motion` 을 존중할 것.** 모션 최소화 설정이면 모든 연출을 끕니다.

---

## 8. 🚨 절대 규칙 — "스크립트가 죽어도 페이지는 살아야 한다"

**2026-08-13, 실제로 배포된 사이트가 이 규칙을 어겨서 망가졌습니다.**

### 무슨 일이 있었나
이전 버전은 `<script type="module">` + import map 으로 `https://esm.sh/animejs` 를 불러왔습니다.
그런데 GitHub Pages 환경에서 그 CDN 로드가 실패하자 **모듈 전체가 실행되지 않았고**, 그 결과:

- 본문이 HTML 자리표시자인 `...` 그대로 남음
- 언어 전환 버튼이 아무 반응 없음

즉 **장식용 라이브러리 하나 때문에 페이지의 핵심 기능이 전부 죽었습니다.**

### 그래서 정한 규칙

| # | 규칙 | 구현 |
|---|---|---|
| 1 | ES 모듈 / import map / 외부 CDN 에 의존하지 않는다 | `script.js` 는 일반 `<script>`. anime.js 는 `anime.min.js` 로 **저장소에 직접 포함(vendored)** |
| 2 | 라이브러리가 없어도 100% 동작한다 | `A()` 헬퍼가 `window.anime` 유무를 확인, 없으면 애니메이션만 건너뜀 |
| 3 | 애니메이션은 **"보이는 상태로 도착"** 만 한다 | 실행되지 않아도 요소는 이미 정상 상태 |
| 4 | 숨김의 기본값은 "보임" 이다 | `[data-reveal]` 은 기본 노출. JS가 살아 있을 때만 `body.reveal-ready` 가 붙어 숨김→등장이 켜짐 |
| 5 | HTML 자리표시자에도 진짜 문구를 넣는다 | `...` 대신 실제 일본어 문장. 만약 JS가 죽어도 읽을 수 있는 페이지가 남음 |

> **수정 후 반드시 확인:** 브라우저 개발자도구 Network 탭에서 `anime.min.js` 를 차단(Block request URL)한 뒤
> 새로고침해서 ① 본문이 보이는지 ② 언어 버튼이 동작하는지 확인하세요.

### 스크롤 등장의 함정
`IntersectionObserver` 는 **교차 상태가 바뀔 때만** 콜백을 줍니다.
`#projects` 같은 앵커로 한 번에 점프하면, 중간 요소는 "화면 아래 → 화면 위" 로 한 프레임에 지나가
상태 변화가 감지되지 않고 **영영 `opacity: 0` 으로 남습니다.**
→ `script.js` 의 `sweep()` 안전망이 스크롤할 때마다 "이미 지나간 요소"를 직접 찾아 드러냅니다. 지우지 마세요.

---

## 9. 검증 체크리스트

수정 후 아래를 모두 통과해야 합니다. (Playwright 로 자동 확인한 항목들입니다)

- [ ] 3개 언어 버튼이 모두 동작하고, 본문·`<html lang>`·`document.title` 이 함께 바뀐다
- [ ] 새로고침해도 마지막 선택 언어가 유지된다
- [ ] 페이지 어디에도 `...` 자리표시자가 남아 있지 않다
- [ ] 콘솔 에러 0건
- [ ] **`anime.min.js` 를 차단해도** 본문이 보이고 언어 전환이 동작한다
- [ ] 맨 아래로 한 번에 점프해도 숨겨진 채 남는 섹션이 없다
- [ ] `#contact` 딥링크로 진입해도 마찬가지다
- [ ] 390px(모바일) / 768px(태블릿) / 1280px 에서 가로 스크롤이 생기지 않는다

---

## 10. 앞으로 손댈 만한 것

- 프로젝트 스크린샷 추가 (현재는 텍스트만 — `word_test/images/` 에 이미 캡처가 있음)
- 일본어 이력서(PDF) 다운로드 버튼
- 라이트 모드 (현재 다크 전용)
- 프로젝트가 3건 이상으로 늘어나면 `.project-row` 를 반복하고 `script.js` 의 `p1`/`p2` 키 구조를
  배열로 리팩터링하는 편이 관리하기 쉽습니다
