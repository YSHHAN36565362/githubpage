# DESIGN.md — 한윤수 포트폴리오 사이트

> 이 저장소(`index.html` / `style.css` / `script.js` / `images/`)의 **설계 기준 문서**입니다.
> 디자인이나 문구를 바꿀 때는 **이 문서를 먼저 고치고, 그다음 코드를 여기에 맞추세요.**
> 코드부터 고치면 다음 사람(= 미래의 나)이 왜 그렇게 되어 있는지 알 수 없게 됩니다.

최종 갱신: 2026-08-14 (v6 — 반복 스크롤 연출 · 카운터 재구성 · 스크린샷 · 버튼 강조)

---

## 1. 목적과 대상

| 항목 | 내용 |
|---|---|
| 목적 | 테크노프로(株式会社テクノプロ コンサルティング・パートナーズ) システムエンジニア 지원용 자기소개 페이지 |
| 읽는 사람 | 일본 기업 채용 담당자(일본어 우선) → 한국 기업·지인(한국어) → 그 외(영어) |
| 성격 | 이력서가 아니라 **"어떤 사람이고 무엇을 만들었는가"를 2~3분 안에 보여주는 페이지** |
| 배포 | GitHub Pages — https://yshhan36565362.github.io/githubpage/ |

**포트폴리오는 2건으로 고정합니다.**
1. 학술논문 연구분야 자동분류 모델 (KoBERT)
2. KMOVE 단어장 앱 (망각곡선 기반)

SafeCity, 구인구직 대시보드 등은 의도적으로 제외했습니다. 자기소개서(ver.7)에서도 포트폴리오를
이 2건으로 좁혔기 때문에 **서류와 사이트의 메시지를 일치**시킵니다.

---

## 2. 디자인 원본 — Jackson 템플릿

`githubpage/jackson-master/` 에 있는 **Colorlib "Jackson" (2018)** 템플릿이 이 사이트의 디자인 원본입니다.
사용자가 "이 스타일로 해달라"고 직접 고른 참고 자료입니다.

**단, 템플릿 파일을 그대로 쓰지 않았습니다.** Bootstrap 3 + jQuery + Waypoints + Flexslider +
Owl Carousel + Modernizr + icomoon(155KB 폰트)까지 딸려오는데, 2건짜리 1페이지 사이트에는 과합니다.
**디자인 언어만 가져와 바닐라 HTML/CSS/JS 로 다시 구현**했습니다.

### 원본에서 가져온 것

| 요소 | 원본 값 | 이 사이트 |
|---|---|---|
| 레이아웃 | 좌측 300px 고정 사이드바 + 우측 본문, 전체 1170px | 동일 (단 float→**CSS Grid**, position:fixed→**sticky**) |
| 강조색 | `#2c98f0` + 6색 로테이션 | 그대로 |
| 유일한 중성색 | `#f2f3f7` | 그대로 (사이드바·타임라인·프로그레스 트랙·아이콘 타일) |
| 폰트 | Quicksand(본문) + Playfair Display(제목) | 그대로 (CJK 폴백 추가) |
| 섹션 제목 | eyebrow 10px/자간5px + 제목 18px/700/대문자/자간5px | 그대로 |
| 섹션 여백 | `padding: 4em 0 9em` (위 적게, 아래 많이) | 그대로 |
| 프로그레스 바 | 6px 트랙 + 끝 손잡이 원 + 위쪽 % 라벨 | 그대로 |
| 타임라인 | 4px 레일 + 40px 원형 아이콘 + 말풍선 카드 | 그대로 |
| 카운터 띠 | 사진 배경 + 어두운 오버레이 + 흰 숫자 | 사진이 없어 **그라데이션 띠**로 대체 |
| 프로젝트 타일 | 300px 높이 + hover 시 **단색 강조색 패널**이 덮임 | 그대로 |
| 버튼 | 12px 대문자/자간1px, radius 2px, 2px 테두리 | 그대로 |

### 원본에서 **고친** 것 (버그이므로 따라 하지 않음)

- `.animate-box` 가 Modernizr 의 `html.js` 클래스에 의존 → 없으면 **본문 전체가 opacity:0**.
  → IntersectionObserver 로 대체하고, **기본값을 "보임"** 으로 뒤집었습니다.
- 사이드바 소셜 아이콘이 `rgba(255,255,255,.3)` — 회색 배경 위 흰색이라 사실상 안 보임.
- `stickyFunction()` 이 로드되지 않은 sticky-kit 을 호출해 매번 에러.
- `background-attachment: fixed` 패럴랙스는 iOS 에서 깨짐 → 쓰지 않았습니다.
- `#colorlib-aside { overflow-y: scroll }` 로 스크롤바 항상 표시 → `auto` 로.
- `data-animate-effect="fadeInTop"` 등 처리되지 않는 오타 값들.

### 원본과 **다르게** 한 것 (콘텐츠 사정)

- 150px 원형 프로필 사진은 원본 템플릿과 동일합니다. 사진을 바꿀 때는 `images/profile.jpg` 를
  교체하고, 얼굴 위치가 어긋나면 `style.css` 의 `.author-img { background-position }` 만 조정하세요.
- **히어로 배경 사진이 없어** 원본의 풀스크린 사진 대신 흰 배경 + 옅은 회색 원 장식으로 처리했습니다.
- 원본의 서비스 6칸 / 블로그 / 아코디언 / 연락 폼은 **넣을 내용이 없어 제거**했습니다.
  (연락 폼은 백엔드가 없으면 동작하지 않으므로 `mailto:` 버튼으로 대체)
- 원본에 없는 **언어 전환 버튼**을 사이드바에 추가했습니다.

---

## 3. 콘텐츠의 출처 (진실의 원천)

새 문구를 쓸 때는 반드시 아래 원본을 먼저 확인하세요. 사이트에서 지어내지 않습니다.

| 섹션 | 근거 파일 |
|---|---|
| **모든 프로필·경력·자격·포트폴리오 표** | **`ver_2_Technopro_desighn_ES_HanYounsu.kmove13.docx` (2026-08-13 제출본)** ← 최우선 |
| 이전 초안(참고용) | `~/Downloads/Claude/Technopro_design_ES_ver.7.txt` |
| 학력 / 기술스택 / 수상 | `GitHub/Introduce/introduce.md` |
| 프로젝트 01 (논문 분류) | `univ-data-science-portfolio/2025-2/kobert-paper-classifier/README.md` |
| 프로젝트 02 (단어장 앱) | `GitHub/word_test/README.md` |

### 톤 규칙
- 자기소개서 문장을 **그대로 붙여넣지 않습니다.** 경어체 이력서 문장은 웹에서 무겁게 읽힙니다.
  원본의 사실과 뉘앙스만 가져와 포트폴리오 톤으로 다시 씁니다.
- **일본어 문체 규칙**: 지원서의 겸양체(`〜ております` / `貴社` / `〜いたします`)를 쓰지 않고,
  일본 IT 포트폴리오 사이트에서 일반적인 **です・ます체**로 씁니다.
  - ○ `改善を続けています` / × `改善を続けております`
  - ○ `作りました` / × `制作いたしました`
  - 회사를 지칭하는 `貴社` 는 사이트에 등장하지 않습니다 (특정 회사용 문서가 아니므로)
  - 섹션 제목은 영어(About / Skills / Works / Career / Contact), 본문은 일본어
  - 프로젝트는 `担当 / 使用技術 / 課題と工夫 / 成果` 라벨로 정리 — 일본 IT 포트폴리오의 표준 구성
- 3개 언어를 **각각 따로 작성**합니다. 기계번역식 직역 금지.
- 논문 분류 프로젝트는 자기소개서에서 "리더로서의 진행 방식" 중심으로 썼지만,
  이 사이트는 포트폴리오이므로 **수치(Acc 0.9691 / Macro-F1 0.9562)를 유지**하고
  리더십 서술을 한 줄 덧붙이는 절충안을 씁니다.

### Skills 막대의 근거
이전 버전에서는 퍼센트를 임의로 넣었지만, **v4 에서 제거**했습니다.
지금은 자기소개서의 `個別的技術紹介書` 표에 있는 **「活用」 구분을 그대로** 씁니다.

| 문서의 활용 구분 | 사이트 표시 | 막대 길이 | 해당 기술 |
|---|---|---|---|
| 開発、使用 | `開発・使用` | 78% | HTML/CSS, Java, JavaScript, MySQL |
| 使用 | `使用` | 52% | Windows, JSP, Python, R, Eclipse, Visual Studio, GitHub, Streamlit |

막대 길이는 이 두 단계를 **시각적으로 구분하기 위한 값**이며, 자기 채점 점수가 아닙니다.
그 취지를 `#skills-lead` 문장으로 화면에도 명시했습니다.

> 참고: 문서상 Python 은 `使用` 으로 되어 있지만 두 프로젝트 모두 Python 기반입니다.
> 문서를 고치실 생각이라면 사이트의 `--pct` 와 라벨(`lv-dev` / `lv-use`)도 함께 맞춰 주세요.

---

## 4. 디자인 토큰

바꿀 일이 있으면 **`style.css` 상단 `:root` 한 곳만** 고치면 전체에 반영됩니다.

```
강조   --primary #2c98f0
6색    --c1 #2c98f0  --c2 #ec5453  --c3 #f9bf3f
       --c4 #a84cb8  --c5 #2fa499  --c6 #4054b2
중성   --gray #f2f3f7   --gray-line #e6e6e6
텍스트 --body rgba(0,0,0,.7)   --dim rgba(0,0,0,.5)   --meta #999
폰트   --font-primary Quicksand + CJK 폴백
       --font-heading Playfair Display + CJK 명조 폴백
치수   --aside-w 300px   --wrap 1170px
```

### 색 사용 규칙
- **색은 배경이 아니라 강조로만 씁니다.** 6색 로테이션은 아이콘, 프로그레스 바,
  타임라인 원 아이콘에만 나타납니다. 큰 면적을 색으로 채우지 않습니다.
- 회색은 `#f2f3f7` **하나뿐**입니다. 두 번째 회색을 만들지 마세요 — 템플릿의 정체성이 무너집니다.
- 위계는 **글자 크기가 아니라 대문자 + 자간(5px) + 아래 여백(4em)** 으로 만듭니다.
  섹션 제목이 18px밖에 안 되는 것은 의도된 것입니다. 키우지 마세요.

---

## 5. 정보 구조

```
[사이드바 300px 고정]              [본문]
  원형 프로필 사진 150px            Hero     이름 · 모토 · CTA 2개
  ハンヨンス / 韓倫洙 / Younsu Han   About    소개 3문단 + プロフィール표 + 강점 4카드
  System Engineer 志望                └ 카운터 띠 (96.9% / 0.956 / No.2 / 12年)
  언어 전환 [日本語][한국어][EN]      Skills   프로그레스 6 + 카테고리별 태그 4블록
  세로 메뉴 6개                     Works    타일 2 + 상세 2 (担当/使用技術/課題と工夫/成果)
  소셜 아이콘 3개                   Career   타임라인 7 (학력·알바·병역·수상·JLPT·연수·졸업)
  copyright                        Contact  Email / GitHub / 희망근무지 + 메일 버튼
```

푸터는 **사이드바 안에** 있습니다(원본과 동일). 본문 하단에 별도 푸터를 만들지 마세요.

---

## 6. i18n (3개 국어)

- 기본 언어 **일본어**. `한국어`, `EN` 버튼으로 전환. 마지막 선택은 `localStorage` 에 저장.
- 번역 텍스트는 외부 JSON 이 아니라 **`script.js` 안의 `translations` 객체**에 둡니다.
  → `fetch` 가 필요 없어 CORS·네트워크 실패의 영향을 받지 않고, 로컬에서 파일을 더블클릭해도 동작합니다.
- 전환 시 `<html lang>` 과 `document.title` 도 함께 바뀝니다 (SEO·스크린리더).
- 고유명사(Python, PyTorch…)와 숫자·날짜는 언어와 무관하게 고정합니다.

> **문구를 고칠 때는 `translations.ja / .ko / .en` 세 곳을 모두 고쳐야 합니다.**
> 한 곳만 고치면 다른 언어에서 옛 문구가 남습니다.

---

## 7. 🚨 절대 규칙 — "스크립트가 죽어도 페이지는 살아야 한다"

**2026-08-13, 실제로 배포된 사이트가 이 규칙을 어겨서 망가졌습니다.**

이전 버전은 `<script type="module">` + import map 으로 `https://esm.sh/animejs` 를 불러왔는데,
GitHub Pages 에서 그 CDN 로드가 실패하자 **모듈 전체가 실행되지 않았고**, 그 결과
본문이 자리표시자 `...` 그대로 남고 언어 버튼이 완전히 죽었습니다.
**장식용 라이브러리 하나 때문에 페이지의 핵심 기능이 전부 죽은 것입니다.**

### 그래서 정한 규칙

| # | 규칙 | 구현 |
|---|---|---|
| 1 | ES 모듈 / import map / 외부 JS CDN 에 의존하지 않는다 | `script.js` 는 일반 `<script>`. **외부 JS 라이브러리 0개** |
| 2 | 라이브러리는 **저장소에 직접 포함**하고, **없어도 동작**해야 한다 | `anime.min.js` (v4.5.0) 를 vendored. CDN 사용 금지. 없으면 CSS + rAF 폴백 |
| 3 | 애니메이션은 **"보이는 상태로 도착"** 만 한다 | 실행되지 않아도 요소는 이미 정상 상태 |
| 4 | 숨김의 기본값은 "보임" 이다 | `.reveal` 은 기본 노출. JS 가 살아 있을 때만 `body.reveal-ready` 가 붙어 숨김→등장이 켜짐 |
| 5 | HTML 자리표시자에도 진짜 문구를 넣는다 | `...` 금지. JS 가 죽어도 읽을 수 있는 페이지가 남음 |
| 6 | 폰트 CDN 은 허용 (실패해도 기능이 아닌 모양만 영향) | Google Fonts + 시스템 CJK 폴백 |

> **수정 후 반드시 확인:** 개발자도구 Network 탭에서 `script.js` 를 차단(Block request URL)한 뒤
> 새로고침해서 ① 본문이 보이는지 ② 링크가 동작하는지 확인하세요.

### 스크롤 등장의 함정
`IntersectionObserver` 는 **교차 상태가 바뀔 때만** 콜백을 줍니다.
`#work` 같은 앵커로 한 번에 점프하면 중간 요소가 "화면 아래 → 화면 위" 로 한 프레임에 지나가
상태 변화가 감지되지 않고 **영영 `opacity: 0` 으로 남습니다.**
→ `script.js` 의 `sweep()` 안전망이 스크롤할 때마다 "이미 지나간 요소"를 직접 찾아 드러냅니다. **지우지 마세요.**

### 캐시 함정
`index.html` 의 `style.css?v=4` / `script.js?v=4` 에서 `?v=` 는 **캐시 무효화용**입니다.
실제로 새 HTML + 옛 CSS 조합으로 화면이 깨진 적이 있습니다.
**CSS 나 JS 를 고칠 때마다 이 숫자를 올리세요.**

---

## 8. 애니메이션 — anime.js 장식 계층

### 왜 다시 넣었나
v3 에서는 사고 여파로 라이브러리를 전부 뺐지만, 사용자가 animejs.com 문서를 근거로
연출 추가를 요청했습니다. **CDN 이 아니라 `anime.min.js` (v4.5.0) 를 저장소에 직접 포함**하고,
**장식 전용 계층**으로만 쓰는 조건으로 다시 넣었습니다.

### 4중 안전장치
1. `<script src="anime.min.js?v=5">` — CDN 아님. 모듈 아님. 로드 실패해도 `script.js` 는 정상 실행.
2. `A()` 게이트웨이 — `window.anime` 가 없거나 `prefers-reduced-motion: reduce` 이면 `null` 반환.
   호출부는 전부 "null 이면 폴백" 으로 작성되어 있습니다.
3. 모든 애니메이션이 **자연 상태(최종 모습)로 도착**합니다. 중간에 끊겨도 화면이 깨지지 않습니다.
4. `body.anime-on` — anime.js 가 살아 있을 때만 붙습니다. CSS 는 이 클래스를 보고
   프로그레스 바의 `transition` 을 꺼서 CSS 와 anime 가 같은 속성을 두 번 움직이는 것을 막습니다.

### 어디에 썼나

| 대상 | anime.js 기능 | 폴백 (anime 없을 때) |
|---|---|---|
| 히어로 제목 | `splitText` + **프로퍼티 키프레임** + `stagger` — 글자가 살짝 떠올랐다 제자리로 | 그냥 텍스트 표시 |
| 히어로 나머지 (eyebrow/역할/설명/버튼) | `stagger` 페이드업, `from` 파라미터 | CSS `.reveal` 페이드업 |
| 히어로 배경 원 장식 | `scale` + `opacity` from | 정적 표시 |
| 카운터 숫자 | **플레인 오브젝트 애니메이션** + `onUpdate` / `onComplete` | `requestAnimationFrame` 카운터 |
| 프로그레스 바 | `width: ['0%', var(--pct)]`, `outExpo` | CSS `transition: width` |
| 타임라인 원형 아이콘 | `scale: {from: 0}`, `outBack` | 정적 표시 |
| 강점 카드 아이콘 | `scale` + `opacity` from, `outBack` | 정적 표시 |

히어로 제목은 animejs.com 의 *Animation* 문서 예제(`splitText` + 프로퍼티 키프레임 + `stagger`)를
그대로 참고하되, 원본의 `loop: true` / `outBounce` / `rotate: '-1turn'` 은 이력용 사이트에
과해서 빼고 **1회 재생 · 짧은 지속시간**으로 조정했습니다. 언어를 바꾸면 이름이 다시 그려지므로
`render()` 직후 `animateHeroTitle()` 을 다시 호출합니다.

### 스크롤 연출은 "왕복 반복" 입니다 (v6)
`IntersectionObserver` 에서 **`unobserve` 를 하지 않습니다.**
화면에 들어오면 `activate()`, 화면 밖으로 나가면 `deactivate()` 가 호출되어,
**위로 올렸다가 다시 내리면 애니메이션이 다시 재생**됩니다.

`deactivate()` 에서 되돌리는 것:
- `.is-visible` 클래스 제거 (CSS 가 다시 숨김 상태로)
- `.progress-wrap` 은 프로그레스 바의 **인라인 `width` 를 지워** CSS 의 `0%` 규칙으로 복귀
  → 다시 들어올 때 anime 가 0 → `--pct` 로 채웁니다
- 카운터는 `runCounter()` 가 항상 0부터 시작하므로 별도 초기화가 필요 없습니다

`.hero-inner` 가 다시 들어올 때는 `animateHeroTitle()` 이 재호출됩니다.
이때 `splitText` 를 그대로 다시 부르면 `<span>` 이 중첩되므로,
**원문을 `data-plain` 속성에 보관**했다가 되돌린 뒤 다시 분할합니다. (이 로직 지우지 마세요)

### 주의사항 (실제로 겪은 것)
- **`transform` 을 중앙정렬에 쓰지 말 것.** anime 가 `scale` 을 애니메이션하면
  `translate(-50%,-50%)` 를 통째로 덮어써 요소가 튕겨나갑니다. → `inset:0; margin:auto` 사용.
- **같은 요소의 `transform` 을 CSS 키프레임과 anime 가 동시에 건드리지 말 것.**
- **`.reveal` 요소 자신을 anime 로 건드리지 말 것.** `.reveal` 의 opacity/transform 은 CSS 담당입니다.
  anime 는 항상 그 **자식 요소**만 애니메이션합니다.
- **버튼 펄스에 `transform: scale` 을 쓰지 말 것.** 확대된 링이 문서 폭을 넓혀
  모바일에서 가로 스크롤이 생겼습니다. → `box-shadow` 로 구현 (레이아웃에 영향 없음).
  추가 방어선으로 `html { overflow-x: clip }` 을 넣어 두었습니다.
- `splitText` 는 스크린리더용 사본 `<span>` 을 하나 추가합니다(시각적으로는 숨김).
  그래서 `innerText` 로 제목을 읽으면 두 번 나옵니다 — 버그가 아닙니다.

---

## 9. 이미지

| 파일 | 원본 | 용도 |
|---|---|---|
| `images/profile.jpg` | 사용자 제공 증명사진(192×264)을 정사각형으로 크롭 후 400×400 | 사이드바 원형 프로필 |
| `images/k-eval.jpg` | `Introduce/KoBERT_Model Evaluation.jpeg` 의 혼동행렬 영역 | 프로젝트 01 상세 |
| `images/w-practice.jpg` | `word_test/images/02-practice.png` | 프로젝트 02 갤러리 |
| `images/w-exam.jpg` | `word_test/images/04-exam.png` | 프로젝트 02 갤러리 |
| `images/w-wordbook.jpg` | `word_test/images/05-wordbook.png` | 프로젝트 02 갤러리 |
| `images/project-kobert.jpg` | `Introduce/KoBERT_Model Evaluation.jpeg` 의 혼동행렬 부분을 잘라 900px/82% 로 압축 | 프로젝트 01 타일 배경 |
| `images/project-wordapp.jpg` | `word_test/images/03-practice-active.png` 의 학습 카드 부분을 잘라 압축 | 프로젝트 02 타일 배경 |

타일은 `background-size: cover; background-position: center top` 입니다.
이미지를 교체할 때는 **가로가 긴 비율(대략 3:2)** 로 잘라야 잘리는 부분이 적습니다.

모바일에서는 hover 가 없으므로 설명 패널을 `rgba(0,0,0,.74)` 로 **항상 표시**합니다.
밝은 사진으로 교체하면 이 값을 더 어둡게 조정해야 흰 글씨가 읽힙니다.

---

## 10. 검증 체크리스트

수정 후 아래를 모두 통과해야 합니다. (Playwright 로 자동 확인한 항목들입니다)

- [ ] 3개 언어 버튼이 모두 동작하고, 본문·`<html lang>`·`document.title` 이 함께 바뀐다
- [ ] 새로고침해도 마지막 선택 언어가 유지된다
- [ ] 페이지 어디에도 `...` / `—` 자리표시자가 남아 있지 않다
- [ ] 콘솔 에러 0건
- [ ] **`script.js` 를 차단해도** 본문이 전부 보인다
- [ ] **`anime.min.js` 를 차단해도** 본문·언어전환·카운터·프로그레스 바가 정상 동작한다
- [ ] OS 의 "모션 줄이기" 설정에서도 콘텐츠가 전부 보이고 카운터가 최종값으로 표시된다
- [ ] 맨 아래로 한 번에 점프해도 숨겨진 채 남는 섹션이 없다
- [ ] 카운터 4개가 최종값(96.9% / 0.956 / No.2 / 5)으로 정확히 멈춘다
- [ ] 프로젝트 타일 hover 시 설명 패널이 덮인다 (데스크톱)
- [ ] 390px(모바일) / 768px / 1440px 에서 가로 스크롤이 생기지 않는다
- [ ] 스크롤을 아래로 내렸다가 **위로 올린 뒤 다시 내렸을 때** 등장 연출·카운터·프로그레스 바가
      다시 재생된다
- [ ] 언어를 여러 번 바꿔도 히어로 제목의 글자 `<span>` 이 중첩되지 않는다
      (`document.querySelector('#hero-name span[data-char] span[data-char]')` 가 null)
- [ ] `제작物` 의 GitHub / Live Demo 버튼이 한눈에 보이고 클릭 가능하다
- [ ] 모바일 햄버거 → 메뉴 열림 → 항목 클릭 시 메뉴가 닫히며 이동한다

---

## 11. 저장소 정리 / 앞으로 할 것

- **`jackson-master/` 는 참고용 템플릿**입니다. 약 4MB(폰트·이미지·sass 캐시 포함)이고
  사이트 동작에는 전혀 쓰이지 않습니다. `.gitignore` 로 제외해 두었습니다.
  GitHub 에 올리고 싶지 않다면 그대로 두고, 굳이 보관할 필요가 없으면 폴더째 옮기세요.
- [ ] 문서상 Python 이 `使用` 인 점이 의도한 것인지 확인 (위 3절 참고)
- [ ] **히어로 배경 사진** — 원본 템플릿은 풀스크린 사진이 전제입니다. 지금은 흰 배경 + 회색 원
      장식으로 대체해 두었습니다. 넣는다면 **가로로 길고(2:1 이상) 밝으며 여백이 많은 사진**이
      좋습니다(히어로 글자가 검은색이라). 예: 작업 책상·노트북 화면, 일본 여행 사진 등
- [ ] **About 섹션 이미지** — 개발 중인 모습이나 학술제 발표 사진이 있으면 설득력이 올라갑니다
- [ ] 학술제 상장/수상 사진이 있으면 Career 타임라인에 넣을 수 있습니다
- [ ] 일본어 이력서(PDF) 다운로드 버튼
- [ ] 프로젝트가 3건 이상으로 늘어나면 `script.js` 의 `p1`/`p2`·`d1`/`d2` 키 구조를
      배열로 리팩터링하는 편이 관리하기 쉽습니다
